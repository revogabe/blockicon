{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "pnpm -F blockicon build",
    "build:core": "pnpm -F core build",
    "build:website": "pnpm -F website build",
    "dev": "run-p dev:*",
    "dev:blockicon": "pnpm -F blockicon dev",
    "dev:website": "pnpm -F website dev",
    "dev:core": "pnpm -F core dev",
    "bucket:upload": "pnpm -F bucket upload"
  },
  "packageManager": "pnpm@10.7.0",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
